---
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";

const allBlogArticles : CollectionEntry<'blog'>[] =await getCollection('blog');

const allTags : string[] = allBlogArticles.flatMap((article:any) => article.data.tags)
const uniqueTags : string[]= [...new Set(allTags)]

---

<footer class="bg-emerald-900 text-white pb-10">
    <div class="container mx-auto mt-10 max-w-screen-xl px-8">
      <div class="grid grid-cols-1 gap-12 sm:grid-cols-2 lg:grid-cols-4">
        <div class="mt-4 md:mt-10">
          <h3 class="text-xl font-semibold mb-4">Σχετικά</h3>
          <p class="text-white text-sm">
           
Το "Φυτά Εντός" είναι ένα blog για λάτρεις των φυτών εσωτερικού χώρου. Δημοσιεύουμε άρθρα, ιστορίες και οδηγούς για τις τελευταίες τάσεις και εξελίξεις στη φροντίδα φυτών.
          </p>
        </div>
        <div class="mt-4 md:mt-10">
          <h3 class="text-xl font-semibold mb-4">Κατηγορίες</h3>
          <ul class="text-white text-sm">
           {uniqueTags.map(tag=>(
            <li class="mb-2">
              <a href={'/articles/tag/'+tag}>{tag}</a>
            </li>
           ))}
          </ul>
        </div>
        <div class="mt-4 md:mt-10">
          <h3 class="text-xl font-semibold mb-4">Επικοινωνία</h3>
          <ul class="text-white text-sm">
            <li class="mb-2">
              <a href="#">Email</a>
            </li>
            <li class="mb-2">
              <a href="#">Twitter</a>
            </li>
            <li class="mb-2">
              <a href="#">Facebook</a>
            </li>
            <li class="mb-2">
              <a href="#">Instagram</a>
            </li>
            <li>
              <a href="#" id="open_preferences_center">Update cookies preferences</a>
            </li>
          </ul>
          
  </footer>